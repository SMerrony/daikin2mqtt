{
    "version": "0.2.0",
    "configurations": [
       {
          "name": "(gdb) Launch",
          "type": "cppdbg",
          "request": "launch",
          "program": "${workspaceFolder}/daikin2mqtt",
          "args": ["-config", "examples/configuration.toml"],
          "stopAtEntry": false,
          "cwd": "${workspaceFolder}",
          "environment": [],
          "MIMode": "gdb",
          "valuesFormatting": "parseText",
          "preLaunchTask": "ada: Build current project",
       }
    ]

 }